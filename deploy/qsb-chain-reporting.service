[Unit]
Description=QSB Chain-of-Command Reporting — floor leads roll REAL worker/floor state up to Wren + Codex (R01 honest, deterministic, no execution)
After=network.target qsb-leadership-relay.service qsb-floor-activity.service
Wants=qsb-leadership-relay.service

[Service]
Type=oneshot
User=ross
WorkingDirectory=/vaults/nvme0/qsb_tower_v1
ExecStart=/usr/bin/python3 /vaults/nvme0/qsb_tower_v1/tools/qsb_worker_chain_reporting.py --once
