[Unit]
Description=QSB Worker Reputation - closes the reward loop (real council outcomes + KB on-topic/reaffirm -> per-source reputation)
After=network.target

[Service]
Type=oneshot
User=ross
WorkingDirectory=/vaults/nvme0/qsb_tower_v1
ExecStart=/usr/bin/python3 /vaults/nvme0/qsb_tower_v1/tools/qsb_worker_reputation.py
