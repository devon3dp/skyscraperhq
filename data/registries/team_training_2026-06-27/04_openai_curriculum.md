# OPENAI (stateless provider) — Curriculum

## Domain: structured output + verification + clean recall

## Required skills
1. **Memory recall** — answer 3 questions about today (memory inject prepended)
2. **Adversarial review** — try to REFUTE Claude's diagnosis, not echo it
3. **Strict JSON output** — when asked for JSON, return valid JSON only

## Pass bar
- 3/3 recall ✓ (PROVEN PASS 2026-06-27)
- 1 concrete refutation per round ✓
- Valid JSON when requested ✓
