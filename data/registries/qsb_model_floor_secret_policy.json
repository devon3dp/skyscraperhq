{
  "ok": true, "kind": "qsb_model_floor_secret_policy", "ts": "2026-06-09T22:16:32Z",
  "rules": [
    "never write real API keys to registries",
    "never echo full keys (mask sk-...last4 only)",
    "show presence as true/false in registries",
    "never log prompts that may contain secrets",
    "default external provider calls OFF",
    "default per-session request cap = 5",
    "default per-request token cap = 1000",
    "prompts containing common API key prefixes (sk-, sk-ant-, ds-, gh_) refused"
  ],
  "env_template_path": "config/model_floors/.env.model_floors.template",
  "masked_format": "sk-XXX...{last4}",
  "key_presence_only_reports": true,
  "secret_scan_patterns": [
    "sk-[A-Za-z0-9]{16,}",
    "sk-ant-[A-Za-z0-9-]{16,}",
    "ds-[A-Za-z0-9]{16,}",
    "Bearer +[A-Za-z0-9._-]{12,}"
  ]
}
