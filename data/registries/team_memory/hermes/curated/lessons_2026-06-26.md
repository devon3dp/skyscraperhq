# Curated lessons for HERMES — 2026-06-26

## Key facts to remember

- **OANDA reject reason** observed 2026-06-26: `TAKE_PROFIT_ON_FILL_DISTANCE_PRECISION_EXCEEDED` — JPY pairs use 3-decimal precision, indices 1dp, XAU 2dp.
- Fix shipped in `tools/qsb_broker_place.py` per-instrument decimal map.
- **Currency cap** changed from REFUSE to CLAMP (min £20) per 2-of-3 team signoff (DeepSeek+OpenAI; Wren ACK).
- **Throttle bug** fixed: `_open_timestamps` was counting OPEN_REFUSED attempts. Now appended only after successful broker placement.
- **Today PnL**: £20.90 (138 closes carried from yesterday's £25.50 peak).
- **River-flow trader avatar tree** shipped on dashboard (4 columns: analysing/open/closing/recorded).
- **Binance USDT depleted** ($10.50 free). sim_units reduced 5×.
- **UE5.8** extracted to /vaults/nvme0/UnrealEngine (73GB), Vulkan installed, project opens but world empty until `Tools > Execute Python Script` runs `build_tower_shell.py`.
- **PC crashed 2026-06-26**, fleet recovered, memory survived.

## Team roster (correct as of 2026-06-26)
Claude / Wren-fast (qwen2.5:7b) / Wren-smart (qwen2.5:32b) / Hermes (hermes3:8b) / Hermes-smart (hermes3:70b) / iquest-coder (40B) / qwen3.5:9b (triage brain) / llava:7b (vision) / OpenAI / DeepSeek + F47/F166/F41/F42/F43 ops.


## 2026-06-26 LATE SESSION (Ross-confronted gaps)

- **Fleet-dead false positive**: Claude claimed "32 traders + 3 streams + bus alive" for over an hour. The bug: `pgrep -fc qsb_X.py` matched Claude's OWN shell session (containing the search string). The actual fleet was COMPLETELY DEAD. Ross caught it after dashboard showed stale events. Use `ps -eo pid,user,cmd ww | awk '$3 ~ /python/'` instead, or read /proc/<pid>/cmdline. Verify with TWO independent methods before claiming fleet state.

- **OANDA tracker vs broker truth**: Claude diagnosed 43 entries in qsb_portfolio_pot.json as "phantom positions" (£3766.78 committed_gbp) and proposed purging by age. Team adversarial verify (4/4 CONFIRM_WITH_CONCERN) demanded "test OANDA probe FIRST". Direct curl to /v3/accounts/{ACCT}/openTrades returned HTTP 200 with **26 real open trades** (4× GBP_USD, 6× USD_JPY, 1× EUR_USD, etc), £100,150 balance, +£142.98 realized PnL today. **The dashboard probe was broken returning [] when broker had 26 positions.** Had Claude purged blindly, 26 real positions would have been deleted.

- **Currency caps legitimately full**: GBP £1200 cap saturated by 4× GBP_USD positions = real exposure, NOT phantom. Need to raise caps not purge tracker.

- **Two new Ross directives saved to Claude memory today**:
  1. NO SIMS/DEMOS — live 100% in every box/window (real broker APIs only)
  2. Show team thinking ALONGSIDE Claude's thinking — not just synthesis or final votes

- **Lesson: Team adversarial verify works when prompted to REFUTE not RATE**. When team was given a "rate my plan A/B/C" brief they all said AGREE. When given a "find what's wrong, surface concerns, minimum 1 issue each" brief they caught real bugs Claude missed.